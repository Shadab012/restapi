let express,APP_PORT;_458‍.w("express",[["default",["express"],function(v){express=v}]]);_458‍.w("./config",[["APP_PORT",["APP_PORT"],function(v){APP_PORT=v}]]);

require("./db/conn")

const st = require('./models/students')


const app = express()

app.post("/students", (req, res)=>{
    res.send("hi there I am shadab")
})

app.listen(APP_PORT, ()=>{console.log(`listning on ${APP_PORT}`)})